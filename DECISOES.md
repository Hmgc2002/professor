# DECISÕES

Uma linha por decisão: **data · o que se decidiu · com que informação · onde vive**.
Uma decisão que não está aqui volta a ser discutida daqui a três meses com menos informação.

| # | Data | Decisão | Com que informação | Onde vive |
|---|---|---|---|---|
| D-001 | 2026-09-15 | O repositório é **público** | O GitHub Pages gratuito exige-o. Consequência aceite: `topicos/` (diagnósticos, respostas, correções) também é público e escreve-se sabendo disso | `CLAUDE.md` §Restrições, `.gitignore` |
| D-002 | 2026-09-15 | O site sai de `main:/docs`, não de uma branch `gh-pages` | Uma só branch; o que se vê no editor é o que está no ar; sem passo de deploy que se possa esquecer | `README.md` |
| D-003 | 2026-09-15 | **HTML autocontido**: CSS e JS inline em cada página, sem build, sem rede, sem CDN | Herdado do `compendio`. Razões: a página abre do disco daqui a dez anos; funciona offline (o aluno estuda no telemóvel sem rede); um CDN que desaparece leva o curso com ele. Custo aceite: ~14 KB de CSS repetido por página | `modelo/`, todas as páginas |
| D-004 | 2026-09-15 | Diagramas em **SVG inline**, não Mermaid | O Mermaid precisa de um script de ~1 MB; embutir isso em cada página contraria D-003, e carregá-lo de CDN contraria D-003 duas vezes. `<pre class="mermaid">` fica reservado para se algum dia houver um renderizador local viável | `PROCESSO.md` §O que um curso tem de ter |
| D-005 | 2026-09-15 | O CSS/JS inline é **gerado a partir de `modelo/base.css` e `modelo/base.js`** por `sincronizar.py`, entre marcadores `<!-- INICIO ESTILO -->` / `<!-- FIM ESTILO -->` | Tensão real: D-003 exige inline, mas «corrigir num sítio é corrigir em todos» torna 20 cópias de CSS uma bomba. A síntese: a **fonte** é um ficheiro só, o **resultado publicado** continua autocontido e sem build para o leitor. Quem abre a página não precisa do script; só quem edita o estilo precisa | `sincronizar.py`, `modelo/base.css`, `modelo/base.js` |
| D-006 | 2026-09-15 | `localStorage` só para conveniência (tema, rascunho de respostas, estado do quiz), sempre em `try/catch` | Modo privado e cookies bloqueados fazem o acesso **atirar**, não devolver `null`. A página tem de funcionar na mesma — só perde a comodidade | `modelo/base.js` |
| D-007 | 2026-09-15 | As respostas do aluno chegam por **copiar-e-colar para um ficheiro Markdown**, não por formulário | Não há backend (D-002) e nenhuma sessão lê o `localStorage` do browser dele. Vantagem inesperada: a entrega fica versionada em Git, com data, e a correção fica ao lado dela | Botão «Copiar as minhas respostas», `topicos/<slug>/respostas/` |
| D-008 | 2026-09-15 | A pedagogia limita-se a técnicas **com evidência**: recuperação, espaçamento, intercalação, exemplo trabalhado + desvanecimento, dificuldades desejáveis, elaboração | Escolher por gosto produz páginas agradáveis e aprendizagem nenhuma. 🔴 «Estilos de aprendizagem» fica explicitamente **fora**, e diz-se porquê na página do método | `docs/metodo.html` |
| D-009 | 2026-09-15 | Intervalos de revisão **+1, +3, +7, +21, +60 dias** | ⚠️ Isto é **convenção**, não um resultado experimental: a evidência sustenta *espaçar e expandir*, não estes números em concreto. Ficam declarados como convenção na própria página | `docs/metodo.html`, `revisao.ics` de cada tópico |
| D-010 | 2026-09-15 | Tópico piloto: **índices B-tree em SQL**, com Postgres como principal e SQLite como alternativa | Escolhido do diagnóstico: é pequeno o suficiente para testar o processo de ponta a ponta, tem mecanismo real (não é lista de factos), e os exercícios correm mesmo. A variante SQLite existe para estudar sem servidor à mão | `topicos/indices-btree-sql/DIAGNOSTICO.md` |
| D-011 | 2026-09-15 | O curso piloto tem **núcleo (lições 0–3) e extensão (4–6)**, marcados como tal | O diagnóstico pediu «as três coisas» (~7 lições) mas só ~1h/semana: 7 lições seguidas são ~8 semanas e o abandono acontece à quinta. Dividir deixa um ponto de chegada honesto a meio do caminho | `docs/indices-btree-sql/index.html` |
| D-012 | 2026-09-15 | O validador é **Python 3.9 da stdlib** (`html.parser`), sem dependências | O macOS traz 3.9.6. Instalar `lxml` ou `beautifulsoup4` para correr um validador cria uma razão para o não correr numa máquina nova. O `axe-core` é a exceção: precisa de `npm`, e o `validar.sh` **avisa e continua** se ele faltar em vez de falhar | `validar.py`, `validar.sh` |
| D-013 | 2026-09-15 | A pesquisa do catálogo procura **dentro das lições**, a partir de um índice **gerado do conteúdo** e embutido inline no `docs/index.html` pelo `sincronizar.py` | A primeira versão filtrava cartões com uma lista de palavras-chave escrita à mão. Testada com 21 termos reais, falhava em 8 — `bitmap`, `vacuum`, `heap fetches`, `work_mem`, `visibility map`, `prefixo`, `correlação`, `estatísticas` — todos conceitos centrais do curso. Uma lista manual nasce incompleta e envelhece a cada lição nova; além disso contradiz a regra da Fase 0 («procurar o termo *e* o que a coisa serve para fazer»), que só funciona se houver o que procurar | `sincronizar.py` (`indice_de_pesquisa`), `modelo/base.js`, marcadores `INICIO/FIM INDICE-PESQUISA` |
| D-014 | 2026-09-15 | A relevância vem de **onde** o termo aparece, não de quantas vezes: título da secção 100 · termo destacado (`code`, `strong`, `h3`, `th`) 30 · título da página 12 · prosa 4. Resultados **agrupados por página**, 6 páginas × 3 secções | Sem pontuação, procurar `bitmap` devolvia 23 secções por ordem alfabética, com «1. Objetivo» à cabeça — encontrava tudo e não servia para nada. Sem agrupamento, uma só lição enchia a lista com as suas onze secções | `modelo/base.js` (`procurar`) |
| D-015 | 2026-09-15 | ⚠️ O índice de pesquisa fica **inline**, ao preço de ~85 kB na página do catálogo | D-003 exige que a página abra do disco sem rede; um ficheiro `.js` separado falharia em `file://` por CORS em vários browsers. **Custo aceite com limite declarado:** ~8 kB de índice por página indexada. Com 5 ou 6 tópicos isto passa de 500 kB e tem de ser repensado — provavelmente índice separado, aceitando que a pesquisa deixa de funcionar a partir do disco. **Quando o catálogo passar de ~40 páginas, esta decisão tem de ser revista** | `docs/index.html` |
| D-016 | 2026-09-15 | Tópico **abstracts-e-resumos** com **núcleo (lições 0–3) e extensão (4–6)** | Diagnóstico: objetivos b (escrever resumos no trabalho), c (ler abstracts) e d (mecanismo), a partir de zero. Três géneros de trabalho antes do mecanismo davam três listas de regras soltas. O núcleo toca os três objetivos; a extensão fecha o critério de sucesso | `topicos/abstracts-e-resumos/DIAGNOSTICO.md`, `docs/abstracts-e-resumos/index.html` |
| D-017 | 2026-09-15 | O abstract científico entra como **modelo de estudo**, não como destino | O aluno não escolheu escrever artigos, mas é o género de resumo com regras escritas e testadas (NISO Z39.14, CONSORT, IMRaD). O mecanismo aprende-se aí e transporta-se para o PR, a proposta e o post-mortem | lições 1 e 2 |
| D-018 | 2026-09-15 | Um **só incidente fictício** atravessa as lições 0, 3, 4, 5 e 6 | Para que a diferença entre dois resumos venha do leitor e não do caso — que é o mecanismo central do tópico | `docs/abstracts-e-resumos/incidente.txt` |
| D-019 | 2026-09-15 | As saídas de código nas lições foram **inseridas por script a partir de execuções reais**; as contagens de palavras e caracteres das amostras passam a ser **conferidas pelo `validar.py`** (`ver_amostras`) | 5 de 17 contagens escritas à mão estavam erradas, num curso que ensina a escrever dentro de um limite. ⚠️ Os scripts que geraram as páginas deste tópico ficaram fora do repositório (sessão de trabalho): a partir daqui, **o HTML publicado é a fonte**, como no tópico piloto | `validar.py`, atributos `data-palavras` / `data-caracteres` / `data-palavras-corpo` |
| D-020 | 2026-09-15 | A regra de commits do repositório («o título diz o que se descobriu») **fica como está**, apesar de contrariar a recomendação do git (≤ 50 caracteres) | Medido: 5 commits, títulos de 111 a 139 caracteres, nenhum com 72 ou menos. É uma troca deliberada; a lição 3 usa-a como exemplo de regras em choque. ⚠️ É uma decisão do dono do repositório: registei a contradição, não a resolvi | `CLAUDE.md` §Commits, `docs/abstracts-e-resumos/03-pr-e-commit.html` |
| D-021 | 2026-09-15 | `validar.sh`: filtro para o aviso do tidy sobre `<ol type>`, e verificação das colunas dos `flashcards.csv` | O tidy 5.8 diz que `type` não é HTML5; a especificação WHATWG lista-o como atributo de `<ol>`. E um `;` dentro de dois cartões partiu-os em quatro colunas sem que nenhum passo o visse | `validar.sh`, passos 2 e 4 |
| D-022 | 2026-09-15 | A posição da opção certa nos quizzes do tópico é **distribuída** na geração | Na primeira versão, 31 das 54 respostas certas estavam na opção b — acertava-se por posição, sem saber a matéria | `docs/abstracts-e-resumos/*.html` |

